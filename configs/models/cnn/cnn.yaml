include:
  - ../../data.yaml

model:
  name: cnn_baseline
  hidden_dim: 128
  branch_dim: 64
  conv_kernel: 5

training:
  backend: torch
  device: auto
  epochs: 50
  batch_size: 64
  lr: 0.0005
  weight_decay: 0.0001
